<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hc.kugou.mapper.MvMapper">
    <sql id="allKeys">
        KuGo_mv.id as id,
        KuGo_mv.mv_name as mv_name,
        KuGo_mv.hc_url as hc_url,
        KuGo_mv.hd_url as hd_url,
        KuGo_mv.bd_url as bd_url,
        KuGo_mv.head_image as head_image,
        KuGo_mv.update_time as update_time
    </sql>
    <sql id="allValues">
        #{id},#{mvName},#{hcUrl},#{hdUrl},#{bdUrl},#{headImage},#{updateTime}
    </sql>
    <select id="findMvByName" resultType="com.hc.kugou.bean.Mv">
        select <include refid="allKeys"/>
        from KuGo_mv
        where KuGo_mv.mv_name like concat(#{mvName},'%')
    </select>
    <insert id="insert">
        insert into KuGo_mv
        values (<include refid="allValues"/>)
    </insert>
</mapper>